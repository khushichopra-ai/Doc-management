# Docker is used ONLY for Qdrant (per architecture spec).
services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: aka_qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"   # REST / dashboard
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
